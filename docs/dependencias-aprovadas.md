# Dependências externas aprovadas

O `CLAUDE.md` deste projeto proíbe usar bibliotecas externas sem que estejam
documentadas. Esta é a lista de exceções aprovadas.

Formato: um nome de pacote PyPI por linha (o nome usado em `pip install <nome>`).
Linhas em branco e iniciadas com `#` são ignoradas.

## Aprovadas

pytest
tdd-guard-pytest
